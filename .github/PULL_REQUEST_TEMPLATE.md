## Summary

What does this change do, and why? Link the issue it addresses, if any.

## Checklist

- [ ] `make check` passes locally (gofmt, go vet, golangci-lint, `go test -race`)
- [ ] `make vulncheck` passes locally, if dependencies changed
- [ ] Tests added or updated for the behavior change
- [ ] `docs/API_REFERENCE.md` updated, if exported API changed
- [ ] `CHANGELOG.md` updated under "Unreleased", if user-visible
- [ ] Breaking changes are called out explicitly below, with a migration
      note

## Breaking changes

None. <!-- Or: describe what breaks and how to migrate. -->
